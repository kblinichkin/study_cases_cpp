#include <vector>

#include <iostream>

// TODO: implement VectorSet and VectorMap and compare performance with std::set, std::map,
//       std::unordered_set, std::unordered_map 

template <typename T>
class VectorSet : private std::vector<T> {
public:
    void insert(const T& value) {

    }

    // ...
};
